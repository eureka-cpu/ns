# ns

Literally the simplest script in existence for changing directories and entering a nix shell.

Try it from anywhere (must have flakes/nix-command features enabled):
```sh
nix run github:eureka-cpu/ns -- <DIR>
```
